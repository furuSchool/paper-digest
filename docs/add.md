以下の機能追加をお願いしたい。

## 1. 過去に配信したことのある論文を二重で配信しないように

- 登録時に on/off ができる (default は on)
- db に arxiv id? とかだけ保存
- 指定件数 (5件など) だけ選択するときに、db にすでに登録されている配信済みの論文を除外

## 2. 引用数によるフィルタリングを行う

- 登録時に on/off (default は off)
- 期間内で、対象の論文のうち、引用数上位 max * 5 件を Semantic Scholar API で取得
    - keyword で絞ってね
    - arxiv で絞ってね
- ランダムに max 件選択 → arxiv から取得

## 3. 登録時にLLMへの要約の要望を入れられるように
- そのままの意味